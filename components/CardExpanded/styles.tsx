import styled from 'styled-components/native';

export const CardExpandedContainer = styled.View({
  height: '100%',
  width: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: 'black',
});

export const CardExpandedCard = styled.View({
  borderRadius: 20,
  backgroundColor: '#2b2a2a',
  height: '80%',
  width: '80%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
});

export const CardText = styled.Text({
  color: 'white',
  fontSize: 16,
  paddingBottom: '30px',
});

export const ModalFlexAligned_View = styled.View({
  // display: 'flex',
  // alignItems: 'flex-start',
  // justifyContent: 'flex-end',
});